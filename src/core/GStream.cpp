#include "black/GStream.h"

char*    GStream::GetDebugText() { return "GStream"; }
uint32_t GStream::Load(GameOSFile* file) { return 0; }
uint32_t GStream::Save(GameOSFile* file) { return 0; }
uint32_t GStream::GetSaveType() { return 0; }
