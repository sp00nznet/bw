#pragma once
// PSysEditor — particle system editor interface
// Struct layout from bw1-decomp (PSysEditor.h)
//
// No struct — just a static method

struct PSysEditorInterface {
    static void Draw3D();
};
