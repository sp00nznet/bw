#pragma once
// GSetup — map setup/loading utilities (all static/free functions)
// No struct body — just method declarations from bw1-decomp

#include <stdint.h>

struct MapCoords;
struct LHOSFile;
struct MapCell;

enum SCRIPT_FEATURE_COMMANDS : uint32_t;
